# Copy to .env and fill in. The .env file is gitignored.
OPENAI_API_KEY=sk-...
# Optional — defaults to gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini
# Optional — port for the proxy server (Vite proxies /api here)
PORT=8787
